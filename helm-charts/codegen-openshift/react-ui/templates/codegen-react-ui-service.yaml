---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "codegen-react-ui.fullname" . }}
  labels:
    {{- include "codegen-react-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "codegen-react-ui.selectorLabels" . | nindent 4 }}
